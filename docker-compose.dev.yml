services:
  backend:
    volumes:
      - ./backend/src:/app/src
    command:
      - uvicorn
      - main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --reload
      - --app-dir
      - src
